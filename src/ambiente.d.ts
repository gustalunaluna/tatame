/**
 * A data do build, injetada pelo Vite (ver `define` em vite.config.ts).
 *
 * Aparece em Configurações › Sobre. Com o app em três lugares ao mesmo tempo —
 * web, Play Store e App Store — "qual versão você está usando?" deixou de ter
 * resposta óbvia, e um aparelho pode ficar semanas atrás da web.
 */
declare const __VERSAO__: string;
